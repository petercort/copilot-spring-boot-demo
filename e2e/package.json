{
  "name": "ecommerce-e2e-tests",
  "version": "1.0.0",
  "description": "Playwright E2E tests for e-commerce microservices demo",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report",
    "test:customers": "playwright test tests/customers.spec.ts",
    "test:products": "playwright test tests/products.spec.ts",
    "test:orders": "playwright test tests/orders.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^20.0.0"
  }
}
