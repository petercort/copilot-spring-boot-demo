# Shell Script Documentation Conventions

When documenting a shell script, include:

- **Usage:** `bash scripts/script-name.sh [args]`
- **Summary:** one sentence describing what it does.
- **Arguments table** (if any parameters).
- **Exit codes** (if non-trivial).
- **Example invocation** with expected output.
